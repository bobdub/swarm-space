/**
 * GUN stub for Vite build compatibility.
 *
 * This stub lets the integrated transport load in environments
 * where the optional GUN dependency is unavailable. The real
 * implementation is dynamically imported at runtime if present.
 */

export default null;
