/**
 * WebTorrent stub for Vite build compatibility
 * 
 * This stub allows the build to succeed when webtorrent is not available.
 * The actual webtorrent library is loaded dynamically at runtime if available.
 */

export default null;
