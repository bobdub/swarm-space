# P2P Signaling Configuration
# Uncomment and set to your deployed signaling server URL for cross-device P2P discovery
# VITE_SIGNALING_URL=wss://your-project.supabase.co/functions/v1/p2p-signaling

# Local-only mode (default)
# When VITE_SIGNALING_URL is not set, P2P discovery only works between tabs in the same browser
